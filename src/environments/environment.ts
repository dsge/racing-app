import { BuildEnvironment } from '../app/models/environment.model';

/**
 * Include this file in the code. It's actual contents are irrelevant though, as during build time
 * this file will be overriden by one of the other environment.*.ts files
 */
export const environment: BuildEnvironment = {} as BuildEnvironment;
